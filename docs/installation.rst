============
Installation
============

At the command line::

    $ easy_install ReconStruct

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv ReconStruct
    $ pip install ReconStruct